services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: talk-talk-backend-dev
    ports:
      - "8765:8765"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - ENVIRONMENT=development
      - DEBUG=true
    env_file:
      - ./backend/.env
    volumes:
      # Mount source code for hot reload (development only)
      - ./backend/app:/app/app:ro
    command: ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8765", "--reload"]
    restart: unless-stopped

networks:
  default:
    name: talk-talk-network-dev
